
[package]
name = "h3-hrx-example"
version = "0.1.0"
edition = "2021"
description = "Use h3-hrx from Rust: prompt to video frames and audio samples"
publish = false

[dependencies]
h3-hrx = { path = "../..", default-features = false }

[[bin]]
name = "minimal"
path = "src/main.rs"
