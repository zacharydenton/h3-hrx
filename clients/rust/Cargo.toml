[workspace]

[package]
name = "h3-minimal"
version = "0.1.0"
edition = "2021"
description = "The h3 crate from Rust: prompt -> frames + samples"

[dependencies]
h3 = { path = "../..", default-features = false }

[[bin]]
name = "minimal"
path = "src/main.rs"

[profile.release]
opt-level = 2
