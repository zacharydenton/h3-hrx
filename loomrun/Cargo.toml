[package]
name = "loomrun"
version.workspace = true
edition.workspace = true
description = "launch one Loom-compiled kernel on the GPU and dump its buffers"

[[bin]]
name = "loomrun"
path = "src/main.rs"

[dependencies]
hrx = { version = "0.1.0", path = "../../hrx.rs", features = ["runner"] }
